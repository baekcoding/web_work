자바의 소스 코드(.java)는 text 문서 이고, 컴파일해서 class 파일로 변경될 예정인 파일

eclipse가 사용하는 공간(java_work)은 임의로 다른 곳에서 파일이나 폴더를 가져와 붙여넣기 하면 안된다.

앱은 운영체제에서 process를 할당 받아야 실행될 수 있다.

기본 데이터 타입에는 null 을 담을 수 없다.(참조 타입에만 가능)

연산의 결과로 실수 값을 얻어내고 싶으면 적어도 하나는 실수여야 한다.

* java 파일에서 class 파일로 컴파일하는 이유 3가지
- 

* new + 클래스명()
: Car 클래스로 물건이 만들어 져서 사물함에 들어간다.